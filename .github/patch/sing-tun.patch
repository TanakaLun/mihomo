--- a/redirect_linux.go	2026-06-20 18:26:43.845972812 +0000
+++ b/redirect_linux.go	2026-06-20 18:53:18.135809698 +0000
@@ -60,6 +60,12 @@
 	if runtime.GOOS == "android" {
 		r.enableIPv4 = true
 		r.iptablesPath = "/system/bin/iptables"
+
+		if len(r.tunOptions.Inet6Address) > 0 {
+			r.enableIPv6 = true
+			r.ip6tablesPath = "/system/bin/ip6tables"
+		}
+
 		userId := os.Getuid()
 		if userId != 0 {
 			r.androidSu = true
@@ -119,7 +125,11 @@
 	if r.customRedirectPort == 0 {
 		var listenAddr netip.Addr
 		if runtime.GOOS == "android" {
-			listenAddr = netip.AddrFrom4([4]byte{127, 0, 0, 1})
+			if r.enableIPv6 {
+				listenAddr = netip.IPv6Unspecified()
+			} else {
+				listenAddr = netip.AddrFrom4([4]byte{127, 0, 0, 1})
+			}
 		} else if r.enableIPv6 {
 			listenAddr = netip.IPv6Unspecified()
 		} else {
--- a/redirect_server.go	2026-06-20 18:26:43.865972812 +0000
+++ b/redirect_server.go	2026-06-21 04:19:41.885872660 +0000
@@ -8,6 +8,7 @@
 	"net"
 	"net/netip"
 	"sync/atomic"
+	"syscall"
 	"time"
 
 	"github.com/metacubex/sing/common/control"
@@ -38,12 +39,45 @@
 
 func (s *redirectServer) Start() error {
 	var listenConfig net.ListenConfig
-	// listenConfig.KeepAlive = C.TCPKeepAliveInitial
 	listenConfig.KeepAlive = 10 * time.Minute
-	listener, err := listenConfig.Listen(s.ctx, M.NetworkFromNetAddr("tcp", s.listenAddr), M.SocksaddrFrom(s.listenAddr, 0).String())
+
+	if s.listenAddr.Is6() {
+		listenConfig.Control = func(network, address string, c syscall.RawConn) error {
+			var opErr error
+			err := c.Control(func(fd uintptr) {
+				opErr = syscall.SetsockoptInt(int(fd), syscall.IPPROTO_IPV6, syscall.IPV6_V6ONLY, 0)
+			})
+			if err != nil {
+				return err
+			}
+			return opErr
+		}
+	}
+
+	var listener net.Listener
+	var err error
+
+	if s.listenAddr.Is4() {
+		listener, err = listenConfig.Listen(s.ctx, "tcp4", M.SocksaddrFrom(s.listenAddr, 0).String())
+	} else if s.listenAddr.Is6() {
+		listener, err = listenConfig.Listen(s.ctx, "tcp6", M.SocksaddrFrom(s.listenAddr, 0).String())
+	} else {
+		ipv4Listener, err4 := listenConfig.Listen(s.ctx, "tcp4", ":0")
+		if err4 == nil {
+			listener = ipv4Listener
+		} else {
+			ipv6Listener, err6 := listenConfig.Listen(s.ctx, "tcp6", ":0")
+			if err6 != nil {
+				return E.Extend(err4, "failed to listen on both IPv4 and IPv6")
+			}
+			listener = ipv6Listener
+		}
+	}
+
 	if err != nil {
 		return err
 	}
+
 	s.listener = listener.(*net.TCPListener)
 	go s.loopIn()
 	return nil
@@ -59,8 +93,6 @@
 		conn, err := s.listener.AcceptTCP()
 		if err != nil {
 			var netError net.Error
-			//goland:noinspection GoDeprecation
-			//nolint:staticcheck
 			if errors.As(err, &netError) && netError.Temporary() {
 				s.logger.Error(err)
 				continue
